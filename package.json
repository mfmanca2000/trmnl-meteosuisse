{
  "name": "trmnl-plugins-mfmanca",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "plugins/*/server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "start": "npm run start --workspace=server"
  }
}
